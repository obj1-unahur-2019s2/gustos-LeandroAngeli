import personas.*
import objetos.*


test "test rosa" {

	assert.notThat(rosa.leGusta(remera))
	assert.that(rosa.leGusta(biblioteca))
	
}


test "test juan" {

		assert.notThat(juan.leGusta(remera))
		assert.that(juan.leGusta(pelota))
}